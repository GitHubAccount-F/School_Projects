1) One thing I learned while doing M2 was how to use transactions to
    prevent bad interleavings and create a functional flights app. One thing 
    that surprised me was creating the tests for the transactions. I intially didn't
    realize that for every output produced by one session, there had to be a complementary
    output produced. This caused me a lot of debugging problems. One question I still have
    is is their alternatives to creating transactions on java besides using the setAutoCommit 
    method?
2) I didn't make any progress on my question from M1(it was about alternatives to
    preventing sql injections). I could probably find an answer to this by searching
    on the internet about sql injections and their solutions to them. 
3) The logic I would use is making the manager field nullable to allow for the special
    case of someone being the ceo. This makes it easy to check if someone is in fact the ceo. 
    For the logic in the table, I won't put "NOT NULL" next to the ManagerEmplID field.
    In java, I would test when that field given is null, and cross reference it if its already in the
    table; If it is, we have an error(assuming we are inserting, but if we are retrieving information
    then it's fine), else we can just add that employee to our table.
4) Took me 24 hours. Only 10 hours were valuable. 